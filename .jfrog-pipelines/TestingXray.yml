resources:
    - name: build
      type: CronTrigger
      configuration:
        interval: '*/10 * * * *'
    - name: scanResult
      type: PropertyBag
      configuration:
          result: 'failure'

pipelines:
    - name: Testing_Xray_2
      configuration:
          jfrogCliVersion: 2
      steps: 
        - name: test
          type: Bash
          configuration:
            integrations:
                - name: jfrogPat
            inputResources:
                - name: build
                  trigger: true
          execution:
            onExecute:
                - touch test.txt
                - echo "Hello World" > test.txt
                - jfrog scan test.txt
            onSuccess:
                - write_output scanResult "result='Success'"
        - name: test_result
          type: Bash
          configuration:
              inputResources:
                  - name: scanResult
              inputSteps:
                  - name: test
          execution:
              onExecute:
                  - scanResult = ${res_scanResult_result}
                  - |
                      if [[scanResult == 'success']] ; then
                          exit 0;
                      fi
                  - exit 1;
